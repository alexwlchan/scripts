#!/usr/bin/env bash

set -o errexit
set -o nounset

if [[ ! -f ~/.rgignore ]]
then
  cat <<EOF > ~/.rgignore
# This file is autogenerated from my 'r' script

!.buildkite/
!.github/
!.obsidian/
!.terraform.lock.hcl
EOF
fi

rg "$@"
